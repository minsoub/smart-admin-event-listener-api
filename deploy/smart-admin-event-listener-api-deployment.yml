apiVersion: apps/v1
kind: Deployment
metadata:
  name: smart-admin-event-listener-api
  namespace: NAMESPACE
  labels:
    app: smart-admin-event-listener-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: smart-admin-event-listener-api
  strategy: {}
  template:
    metadata:
      labels:
        app: smart-admin-event-listener-api
      containers:
        - name: smart-admin-event-listener-api
          image: AWS_ECR_URI
          ports:
            - containerPort: 8080
          resources: {}

